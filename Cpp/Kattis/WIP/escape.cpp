#include <bits/stdc++.h>
using namespace std;
typedef long long l;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    l t;
    cin >> t;
    for(l i = 0; i < t; i++){
        l n, target;
        cin >> n >> target;
        for(l j = 0; j < n; j++){
            l val;
            cin >> val;
        }
        
    }
}
