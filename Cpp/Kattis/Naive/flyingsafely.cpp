#include <bits/stdc++.h>
typedef long long l;

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    l test_cases;
    std::cin >> test_cases;

    for(l i = 0; i < test_cases; ++i){
        l n, m;
        std::cin >> n >> m;

        for(l j = 0; j < m; j++){
            l a, b;
            std::cin >> a >> b;
        }
        std::cout << (n-1) << std::endl;
    }

}
